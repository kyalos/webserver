{
  "name": "webserver",
  "version": "1.0.0",
  "description": "A basic HTTP web-server application which can listen on a configurable TCP port and serve both static HTML and dynamically generated HTML",
  "main": "app.js",
  "scripts": {
    "dev": "nodemon ./app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kyalos",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
